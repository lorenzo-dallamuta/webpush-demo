{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "start": "vite preview",
    "preview": "pnpm build && pnpm start"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "vite": "^5.1.4"
  }
}